/**
 * Compact app chrome (dense toolbar + aligned drawer).
 * Keep in sync: `header.tsx` Toolbar, `main-layout` `mt`/`minHeight`, `sidebar` drawer `top`/`height`.
 */
export const APP_BAR_HEIGHT_PX = 48

/** Permanent desktop drawer — must match `MainLayout` `ml` / `width` for open vs collapsed */
export const DRAWER_WIDTH_EXPANDED_PX = 236
export const DRAWER_WIDTH_COLLAPSED_PX = 64
