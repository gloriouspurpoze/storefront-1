/** Default MUI Toolbar height; keep sidebar + `MainLayout` main region in sync with fixed `AppBar` */
export const APP_BAR_HEIGHT_PX = 64

/** Permanent desktop drawer — must match `MainLayout` `ml` / `width` for open vs collapsed */
export const DRAWER_WIDTH_EXPANDED_PX = 280
export const DRAWER_WIDTH_COLLAPSED_PX = 72
